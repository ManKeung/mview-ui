import config from '../conifg'

export default {
    loadingText: '正在加载',
    image: '',
    loadingMode: 'circle',
    loading: false,
    bgColor: '#ffffff',
    color: config.color['m-info'],
    fontSize: 16,
    loadingColor: config.color['m-info'],
    loadingInactiveColor: '',
    iconSize: ''
}
